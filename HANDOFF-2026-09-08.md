# HANDOFF — Gravity Sandbox (sessione compattata)
**Da:** sessione WebUI `48b80ae7717b` ("Lista Proprietà Stella e Salvataggio Preset"), 864 messaggi, ~315K token — resa non più servibile dal modello free cache-only di tokenrouter (HTTP 503 `cache_only_cold`, la richiesta è troppo grande per stare in cache).
**Data:** 2026-09-08. **Backup completo:** `~/.hermes/webui/sessions/48b80ae7717b.json.bak-compattazione-20260908` (9.45 MB, reversibile).

## Progetto
Sandbox di gravità N-body, Bevy 0.19 + Avian 0.7, WASM servita su porta 8081 (test su iPhone Safari + Mac). Regole fisse: UI = Bevy nativa (no HTML overlay), debug senza console browser (curl + log), target-dir condiviso `~/rust-target/gravity` + sccache, version bump + commit + push a ogni cambiamento, non rimuovere feature funzionanti.

## Stato al momento della compattazione (TUTTO VERIFICATO SUL REPO)
- Ultimo commit: `5c1f84e` "v0.14.79: separazione DEFINITIVA alone/luce pianeti NELLO SHADER (select su normal.a)", tree pulito, push fatto, **v0.14.79 servita su :8081**.
- Ticket 20 completato e chiuso: **Planet Light** (illuminazione pianeti) e **Halo Brightness** (alone stella) ora sono **due parametri separati nello shader** (select su `normal.a`), entrambi salvati nel preset, preset vecchi compatibili. Confermato funzionante da Davide.
- Erano già stati risolti in sessione: cache Safari iOS (rinomina asset `gravity_sandbox_pNN.*`, busting via `?debug` e verifiche hash), crash B0001, fix save-preset 501, separazione alone/luce (due round: v0.14.78 parametri separati, v0.14.79 shader definitivo).

## RICHIESTA SOSPESA (da riprendere per prima cosa)
> "Ora i coni di ombra vengono creati solo se la distanza pianeta-stella è inferiore al parametro radius, bisogna cambiare in modo che i coni d'ombra vengano creati se la distanza è minore di radius+fade"

**Dove toccare (verificato nel codice):**
- `src/systems/shadow.rs` ~riga 229: gate attuale `li.distance_to_star > body.radius` (il cono si crea solo se il pianeta è PIÙ LONTANO del proprio raggio dalla stella). Il fix richiesto: creare il cono se `distance < star_light_radius + fade_width`.
  - Attenzione al segno: ora è `>` (più lontano del raggio del corpo), la richiesta è un limite superiore `distance < radius + fade` sul raggio di LUCE della stella.
  - `fade_width` esiste già: campo in `src/systems/light.rs` (Component `LightSettings`, es. riga 260 `mat.body_radius`... in realtà `LightSettings { fade_width }`), editabile nel pannello come `light_fade` (`src/systems/ui.rs:431,702,823`), e applicato dalla bridge (`src/systems/firefly_bridge.rs:555,574`).
  - `shadow_cone_vertices()` già gestisce il caso degenere `dist <= radius` restituendo `None` (riga ~281: `if radius <= 0.0 || dist <= radius { return None }`) — valutare se il gate interno debba seguirlo.

**Vincoli per il fix (da HERMES.md + sessione):** UI Bevy nativa, WASM build verificata dopo la modifica, version bump (es. v0.14.80) + commit + push, rigenerare i sidecar `.gz`/`.size` del deploy e rinominare l'asset (`_pNN`) per il busting cache iOS, 1 sola versione servita per i test.

## Convenzioni apprese in sessione (non presenti altrove)
- Verità runtime su iOS/Mac = marker/hash byte + 'Creating new window...' + prima riga panic, NON HTTP/cache (già in memoria Hermes).
- Davide chiede spesso screenshot per fine-tuning UI e dà feedback pixel-preciso; "una cosa alla volta" (separa i problemi accoppiati, uno per volta).
- L'agente in quella sessione usava come modello `z-ai/glm-5.3-free`; se un giorno serve riprendere LA STESSA sessione, il prompt dev'essere piccolo (cache-only). Meglio: nuova sessione + questo handoff.

## Suggerimenti per la sessione successiva
**Skill da caricare:** `bevy-0.19-gravity-sandbox` (API specifiche del progetto), `bevy-wasm-deployment` (deploy/busting cache), `bevy-wasm-artifact-verification` (verifica hash se Davide segnala "versione vecchia"), `bevy-wasm-remote-debugging` (crash senza console).

**Primo messaggio consigliato alla nuova sessione:** "Riprendi dal handoff in `~/workspace/gravity-sandbox/HANDOFF-2026-09-08.md` (sessione compattata) e implementi la modifica ai coni d'ombra: crearli se la distanza pianeta-stella è minore di radius+fade."
